2026-03-28 – **Post-cleanup verification + writer audit (read-only):** `docs/SCRYFALL_CACHE_POST_CLEANUP_VERIFICATION.md` — operator checklist, cron/import safety table for `scryfall_cache` writers; `db/scryfall_cache_post_cleanup_health_counts.sql` — aggregate health metrics; `db/scryfall_cache_health_audit.sql` header cross-links. **Files:** `docs/SCRYFALL_CACHE_POST_CLEANUP_VERIFICATION.md`, `db/scryfall_cache_post_cleanup_health_counts.sql`, `db/scryfall_cache_health_audit.sql`, `frontend/CHANGELOG.txt`.

2026-03-28 – **Final small-bucket review (quoted/bracketed/import junk):** `scripts/generate-scryfall-cache-final-small-bucket.mjs` + `npm run generate:scryfall-cache-final-small-bucket` — `tmp/scryfall-cache-final-small-bucket-review.json`, `db/preview_scryfall_cache_final_small_bucket.{sql,json}`. **Files:** `frontend/scripts/generate-scryfall-cache-final-small-bucket.mjs`, `frontend/package.json`, `db/SCRYFALL_CACHE_NAME_AUDIT_SCRIPT.md`, `frontend/CHANGELOG.txt`.

2026-03-28 – **build-scryfall-cache-remaining-ai-reviewed:** `scripts/build-scryfall-cache-remaining-ai-reviewed.mjs` + `npm run build:scryfall-cache-remaining-ai-reviewed` — conservative `tmp/scryfall-cache-remaining-ai-reviewed.json` from rich export. **Files:** `frontend/scripts/build-scryfall-cache-remaining-ai-reviewed.mjs`, `frontend/package.json`, `db/SCRYFALL_CACHE_NAME_AUDIT_SCRIPT.md`, `frontend/CHANGELOG.txt`.

2026-03-28 – **Remaining unmatched: rich AI export + reviewed SQL preview:** `export-scryfall-cache-remaining-ai-review.mjs` + `generate-preview-scryfall-cache-remaining-ai-reviewed-sql.mjs` — `tmp/scryfall-cache-remaining-ai-review.{json,csv,prompt.txt}`, `db/preview_scryfall_cache_remaining_ai_reviewed.{sql,json}`. **Files:** `frontend/scripts/export-scryfall-cache-remaining-ai-review.mjs`, `frontend/scripts/generate-preview-scryfall-cache-remaining-ai-reviewed-sql.mjs`, `frontend/package.json`, `db/SCRYFALL_CACHE_NAME_AUDIT_SCRIPT.md`, `frontend/CHANGELOG.txt`.

2026-03-28 – **Cleanup-preview deterministic repairs SQL (safe):** `scripts/generate-preview-scryfall-cache-cleanup-repairs-safe.mjs` + `npm run generate:preview-scryfall-cache-cleanup-repairs-safe` — `db/preview_scryfall_cache_cleanup_preview_repairs_safe.{sql,json}` from audit + cleanup preview (`repair_to_canonical` only). **Files:** `frontend/scripts/generate-preview-scryfall-cache-cleanup-repairs-safe.mjs`, `frontend/package.json`, `db/SCRYFALL_CACHE_NAME_AUDIT_SCRIPT.md`, `frontend/CHANGELOG.txt`.

2026-03-28 – **AI cleanup SAFE SQL (normalized PK):** `scripts/generate-preview-scryfall-cache-ai-cleanup-safe.mjs` + `npm run generate:preview-scryfall-cache-ai-cleanup-safe` — `db/preview_scryfall_cache_ai_cleanup_safe.{sql,json}`; merge/rename/delete preview with read-only Supabase; deprecated display-case `generate-preview-scryfall-cache-ai-cleanup-sql.mjs`. **Files:** `frontend/scripts/generate-preview-scryfall-cache-ai-cleanup-safe.mjs`, `frontend/package.json`, `db/SCRYFALL_CACHE_NAME_AUDIT_SCRIPT.md`, `frontend/CHANGELOG.txt`.

2026-03-28 – **AI-reviewed cleanup SQL preview:** `scripts/generate-preview-scryfall-cache-ai-cleanup-sql.mjs` + `npm run generate:preview-scryfall-cache-ai-cleanup-sql` — reads `tmp/scryfall-cache-ai-reviewed.json`, writes `db/preview_scryfall_cache_ai_cleanup.sql` (BEGIN/ROLLBACK, no DB). **Files:** `frontend/scripts/generate-preview-scryfall-cache-ai-cleanup-sql.mjs`, `frontend/package.json`, `db/preview_scryfall_cache_ai_cleanup.sql`, `db/SCRYFALL_CACHE_NAME_AUDIT_SCRIPT.md`, `frontend/CHANGELOG.txt`.

2026-03-28 – **AI review export (unmatched cache names):** `scripts/export-scryfall-cache-ai-review.mjs` + `npm run export:scryfall-cache-ai-review` — reads audit + cleanup preview, optional default_cards; writes `tmp/scryfall-cache-ai-review-input.json`, `tmp/scryfall-cache-ai-review-summary.csv`, `tmp/scryfall-cache-ai-review-prompt.txt` (no DB/SQL). **Files:** `frontend/scripts/export-scryfall-cache-ai-review.mjs`, `frontend/package.json`, `db/SCRYFALL_CACHE_NAME_AUDIT_SCRIPT.md`, `frontend/CHANGELOG.txt`.

2026-03-28 – **SQL plan (import_set_number_junk safe):** `scripts/preview-scryfall-cache-sql-plan-import-set-number-junk-safe.mjs` + `npm run preview:scryfall-cache-sql-plan-import-set-number-junk-safe` — `tmp/scryfall-cache-sql-plan-import-set-number-junk-safe.{sql,json}`. **Files:** `frontend/scripts/preview-scryfall-cache-sql-plan-import-set-number-junk-safe.mjs`, `frontend/package.json`, `db/SCRYFALL_CACHE_NAME_AUDIT_SCRIPT.md`, `frontend/CHANGELOG.txt`.

2026-03-28 – **Import set/number junk cleanup preview:** `scripts/preview-scryfall-cache-import-set-number-junk.mjs` + `npm run preview:scryfall-cache-import-set-number-junk` — `tmp/scryfall-cache-preview-import-set-number-junk.{json,csv}`. **Files:** `frontend/scripts/preview-scryfall-cache-import-set-number-junk.mjs`, `frontend/package.json`, `db/SCRYFALL_CACHE_NAME_AUDIT_SCRIPT.md`, `frontend/CHANGELOG.txt`.

2026-03-28 – **SQL plan (bracketed_name safe):** `scripts/preview-scryfall-cache-sql-plan-bracketed-name-safe.mjs` + `npm run preview:scryfall-cache-sql-plan-bracketed-name-safe` — `tmp/scryfall-cache-sql-plan-bracketed-name-safe.{sql,json}`. **Files:** `frontend/scripts/preview-scryfall-cache-sql-plan-bracketed-name-safe.mjs`, `frontend/package.json`, `db/SCRYFALL_CACHE_NAME_AUDIT_SCRIPT.md`, `frontend/CHANGELOG.txt`.

2026-03-28 – **Bracketed_name cleanup preview:** `scripts/preview-scryfall-cache-bracketed-name.mjs` + `npm run preview:scryfall-cache-bracketed-name` — `tmp/scryfall-cache-preview-bracketed-name.{json,csv}`. **Files:** `frontend/scripts/preview-scryfall-cache-bracketed-name.mjs`, `frontend/package.json`, `db/SCRYFALL_CACHE_NAME_AUDIT_SCRIPT.md`, `frontend/CHANGELOG.txt`.

2026-03-28 – **SQL plan (csv_pollution safe):** `scripts/preview-scryfall-cache-sql-plan-csv-pollution-safe.mjs` + `npm run preview:scryfall-cache-sql-plan-csv-pollution-safe` — `tmp/scryfall-cache-sql-plan-csv-pollution-safe.{sql,json}`. **Files:** `frontend/scripts/preview-scryfall-cache-sql-plan-csv-pollution-safe.mjs`, `frontend/package.json`, `db/SCRYFALL_CACHE_NAME_AUDIT_SCRIPT.md`, `frontend/CHANGELOG.txt`.

2026-03-28 – **CSV pollution cleanup preview:** `scripts/preview-scryfall-cache-csv-pollution.mjs` + `npm run preview:scryfall-cache-csv-pollution` — `csv_pollution` bucket only; `tmp/scryfall-cache-preview-csv-pollution.{json,csv}`. **Files:** `frontend/scripts/preview-scryfall-cache-csv-pollution.mjs`, `frontend/package.json`, `db/SCRYFALL_CACHE_NAME_AUDIT_SCRIPT.md`, `frontend/CHANGELOG.txt`.

2026-03-28 – **Delete preview (partial_bracket_pollution):** `scripts/preview-scryfall-cache-delete-partial-brackets.mjs` + `npm run preview:scryfall-cache-delete-partial-brackets` — `tmp/scryfall-cache-delete-partial-brackets.{sql,json}`. **Files:** `frontend/scripts/preview-scryfall-cache-delete-partial-brackets.mjs`, `frontend/package.json`, `db/SCRYFALL_CACHE_NAME_AUDIT_SCRIPT.md`, `frontend/CHANGELOG.txt`.

2026-03-28 – **Last-repairs SQL preview:** `scripts/preview-scryfall-cache-sql-plan-last-repairs.mjs` + `npm run preview:scryfall-cache-sql-plan-last-3` — audit + preview, repairs only, `tmp/scryfall-cache-sql-plan-last-3.{sql,json}`. **Files:** `frontend/scripts/preview-scryfall-cache-sql-plan-last-repairs.mjs`, `frontend/package.json`, `db/SCRYFALL_CACHE_NAME_AUDIT_SCRIPT.md`, `frontend/CHANGELOG.txt`.

2026-03-28 – **Final-safe SQL plan:** `scripts/preview-scryfall-cache-sql-plan-final-safe.mjs` + `npm run preview:scryfall-cache-sql-plan-final-safe` — reads latest audit + cleanup preview, filters to audit `unmatched` PKs, queries DB, writes `tmp/scryfall-cache-sql-plan-final-safe.{sql,json}`. **Files:** `frontend/scripts/preview-scryfall-cache-sql-plan-final-safe.mjs`, `frontend/package.json`, `db/SCRYFALL_CACHE_NAME_AUDIT_SCRIPT.md`, `frontend/CHANGELOG.txt`.

2026-03-28 – **SQL plan preview (read-only):** `scripts/preview-scryfall-cache-sql-plan.mjs` + `npm run preview:scryfall-cache-sql-plan` — reads cleanup preview JSON + `SELECT` involved `scryfall_cache` rows; classifies merge/rename/delete/skip; writes `tmp/scryfall-cache-sql-plan.{json,csv,sql}` (SQL ends with `ROLLBACK`); `db/SCRYFALL_CACHE_CLEANUP_SQL_PREVIEW.md` + audit doc cross-link. **Files:** `frontend/scripts/preview-scryfall-cache-sql-plan.mjs`, `frontend/package.json`, `db/SCRYFALL_CACHE_NAME_AUDIT_SCRIPT.md`, `db/SCRYFALL_CACHE_CLEANUP_SQL_PREVIEW.md`, `frontend/CHANGELOG.txt`.

2026-03-28 – **Preview mapper v2 (second pass):** `preview-scryfall-cache-cleanup.mjs` — pass-2 strips (price, collector+rarity, foil, role tags, `/`→`//`), curly-quote preprocess, unique face→full map from `default_cards`, `matched_by_rule` + `repair_pass` columns, summary `repair_pass_1` / `repair_pass_2`; conservative prompt/sentence delete. **Files:** `frontend/scripts/preview-scryfall-cache-cleanup.mjs`, `db/SCRYFALL_CACHE_NAME_AUDIT_SCRIPT.md`.

2026-03-28 – **Preview cleanup mapper (read-only):** `scripts/preview-scryfall-cache-cleanup.mjs` + `npm run preview:scryfall-cache-cleanup` — reads audit JSON, rebuilds canonical set from bulk (or `--canonical-json`), proposes repair/delete/manual; outputs `tmp/scryfall-cache-cleanup-preview.{json,csv}`. **Files:** `frontend/scripts/preview-scryfall-cache-cleanup.mjs`, `frontend/package.json`, `db/SCRYFALL_CACHE_NAME_AUDIT_SCRIPT.md`, `docs/SCRYFALL_CACHE_REBUILD_AUDIT.md`.

2026-03-28 – **Audit script (read-only):** `scripts/audit-scryfall-cache-names.mjs` + `npm run audit:scryfall-cache-names` — DB `name` vs latest `default_cards` canonical set; JSON/CSV under `tmp/`; `db/SCRYFALL_CACHE_NAME_AUDIT_SCRIPT.md`. **Files:** `frontend/scripts/audit-scryfall-cache-names.mjs`, `frontend/package.json`, `frontend/.gitignore`, `db/SCRYFALL_CACHE_NAME_AUDIT_SCRIPT.md`, `docs/SCRYFALL_CACHE_REBUILD_AUDIT.md`.

2026-03-28 – **scryfall_cache incomplete repair:** `isScryfallCacheRowIncomplete` + `POST /api/cron/scryfall-cache-incomplete-repair` (merge upsert, batched collection calls, resume `after`); `db/SCRYFALL_CACHE_INCOMPLETE_REPAIR.md`.

2026-03-28 – **lightweight-scryfall + bulk-jobs/scryfall-import:** upserts use `buildScryfallCacheRowFromApiCard` (full Phase 2A row); dedupe key `String(row.name)`; schema probe uses same builder. **Files:** `app/api/cron/lightweight-scryfall/route.ts`, `app/api/bulk-jobs/scryfall-import/route.ts`.

2026-03-28 – **scryfall_cache PK hardening (additive):**
- `lib/server/scryfallCacheRow.ts` — `buildScryfallCacheRowFromApiCard` returns `null` if top-level `card.name` is empty; optional `ScryfallCacheWriteContext`; `mergeScryfallCacheRowFromApiCard` logs DB PK vs normalized API `card.name` (plus `set` / `collector_number` / `route`). JSDoc: canonical `normalizeScryfallCacheName` vs bulk-price `norm` (near-match).
- `lib/server/scryfallCache.ts` — skip cache upsert when collection lookup key ≠ normalized API `card.name` (getDetailsForNamesCached, getEnrichmentForNames).
- `lib/deck/inference.ts`, `app/api/cron/bulk-scryfall/route.ts`, deck save/update/page, recompute-archetypes — handle null rows; inference/recompute use imported `normalizeScryfallCacheName`.
- `lib/server/priceSnapshotFromScryfallBulk.ts` — use `normalizeScryfallCacheName` for aggregation keys.
- `bulk-jobs-server/server.js` — skip cards with empty `card.name`; empty-batch guard before upsert.
- `db/scryfall_cache_health_audit.sql` — read-only duplicate/drift/missing-field queries.
- `app/api/admin/data/scryfall-cache-health/route.ts` — admin GET snapshot (counts + SQL pointer).
- `docs/SCRYFALL_CACHE_REBUILD_AUDIT.md` — Operational hardening section.
- `app/api/cron/lightweight-scryfall/route.ts`, `app/api/bulk-jobs/scryfall-import/route.ts` — PK via `normalizeScryfallCacheName` + `name_norm`; trim empty names.

2026-03-26 – `POST /api/deck/transform` multi-mode: canonical transform intents (`refine_imported_deck`, `improve_mana_base`, `tighten_curve`, `add_interaction`, `lower_budget`, `more_casual`, `more_optimized`, `fix_legality`, `transform_template`, `general`) in `lib/deck/transform-intent.ts` with aliases + fail-open to `general`; stronger modify-in-place system/user prompts in `generation-input.ts`; deterministic warnings via `lib/deck/transform-warnings.ts` (source off-color lines, color-filter drops); summary uses `summarizeTransformIntent` + optional avg CMC from `previewFacts`.

2026-03-26 – `buildMode` / `refinement` prompt directives: stronger operational text in `lib/deck/generation-input.ts` (`buildModePromptDirective`, `refinementPromptDirective`, `formatBuildModeAndRefinementDirectives`); case-insensitive keys; optional `buildMode` + `refinement` on `POST /api/deck/transform` body; compliance reminder when set. Parser/output unchanged (still plain 100-line lists).

2026-03-26 – Deck generation API (additive, backward compatible):
- `POST /api/deck/generate-from-collection` — optional structured body fields (`generationIntent`, `seedCard`, `buildMode`, `refinement`, `sourceDeckText`, `ideaText`, `templateContext`, `notes`) normalized in `lib/deck/generation-input.ts`; legacy `collectionId` / `commander` / `playstyle` / `powerLevel` / `budget` / `format` unchanged. Additive response field `previewFacts` (curve/stats via `buildDeckContextSummary` / `deck_facts`) when enrichment succeeds.
- `POST /api/deck/transform` — Commander-only; requires `sourceDeckText`; optional `transformIntent` or `transformType`, `format`, `commander`, `powerLevel`, `budget`, `constraints`, `notes`. Shares durable rate-limit bucket with generate-from-collection. Helpers: `lib/deck/generation-helpers.ts`, `lib/deck/generation-preview-facts.ts`. Feature caps: `DECK_TRANSFORM_*` aliases in `lib/feature-limits.ts`.

2026-03-26 – `POST /api/decks/health-report` injects deterministic `buildDeckFacts` (after `enrichDeck` → `tagCards`) into the LLM prompt; same JSON response shape. Export `resolveCommanderFromEnriched` from `deck-context-summary.ts` for reuse (`app/api/decks/health-report/route.ts`, `lib/deck/deck-context-summary.ts`). Docs: `docs/WEBSITE_SCRYFALL_META_TAG_AUDIT.md` updated for health-report grounding.

2025-03-24 – Phase 4I: `isLandForDeck` / `isCreatureForDeck` prefer `is_land` / `is_creature` when set, else legacy `type_line` substrings; `buildDeckFacts` land split + role rules (`checkRamp` mana_dork, `checkFinisher`, `checkComboEngine`, `checkFixing`) (`lib/deck/card-role-tags.ts`, `lib/deck/deck-facts.ts`).

2025-03-24 – Phase 4G: `getDetailsForNamesCacheOnly` loads `keywords`; `moduleDetection` ORs `hasOracleKeyword` (Cascade, Storm, Landfall, Dredge) with existing oracle checks; `composeSystemPrompt` uses `CachedCard` for norm map (`lib/server/scryfallCache.ts`, `lib/prompts/moduleDetection.ts`, `lib/prompts/composeSystemPrompt.ts`).

2025-03-24 – Phase 4F: `lib/deck/inference.ts` — `sfIsLand`, `sfIsCreature`, `sfIsInstant`, `sfIsPlaneswalker` prefer `SfCard` type flags with `type_line` regex fallback; used in `inferDeckAim`, `tagCardRoles`, `analyzeCurve`, `analyzeManabase`, `inferDeckContext` land count.

2025-03-24 – Phase 4E: `POST /api/cards/batch-metadata` selects and passes through optional `legalities`, `keywords`, `colors`, P/T, `loyalty`, `is_*` from `scryfall_cache`; memo key `batch-metadata:v2:` (`app/api/cards/batch-metadata/route.ts`).

2025-03-24 – Phase 4D: `SfCard` + `fetchCard` / `fetchCardsBatch` read optional Phase 2A fields from `scryfall_cache` (`keywords`, `colors`, P/T, `loyalty`, `is_*`); `mapApiCardExtras` for API path; `SC_CACHE_INFERENCE_SELECT` (`lib/deck/inference.ts`).

2025-03-24 – Phase 4C: recommendations deck API uses `cacheNameNorm` (same as `scryfall_cache` PK) for legality `.in('name', …)`, legal set membership, deck/collection checks, commander identity keys (`app/api/recommendations/deck/[id]/route.ts`).

2025-03-24 – Phase 4B: mulligan `getTypeLinesForNames` + `isLandFromLookup` use `scryfall_cache.is_land` when set, else existing `type_line` / known-name logic (`lib/mulligan/card-types.ts` only).

2025-03-24 – Phase 4A: `getEnrichmentForNames` reads cached power, toughness, loyalty, keywords, colors:
- `lib/server/scryfallCache.ts` — extended `.select`, `EnrichmentRow`, DB + API branch mapping; `coerceStringArray` for jsonb arrays.
- `lib/deck/deck-enrichment.ts` — `EnrichedCard` pass-through for same fields.

2025-03-24 – Price snapshots: cron aligned with Scryfall bulk (product choice #2):
- `lib/server/priceSnapshotFromScryfallBulk.ts` — shared `default_cards` download, median USD/EUR per `name_norm`, GBP via FX; upsert + 60d cleanup.
- `app/api/cron/price/snapshot/route.ts` — uses shared runner (no longer prefers `price_cache`); `maxDuration` 600s.
- `app/api/bulk-jobs/price-snapshot/route.ts` — delegates to shared runner.
- `app/admin/data/page.tsx` — Job 3 copy: production `CRON_URL` should hit `/api/cron/price/snapshot`; same pipeline as local bulk button; “What it does” / dependencies updated (snapshots do not read `price_cache`).

2025-03-24 – Phase 3 backfill loop script (run to completion):
- `frontend/run-phase3-backfill-loop.mjs` — `no_rows_needing_backfill` only stops when cursor does not advance; otherwise advances `after` and continues. `merged=0` streak only when `candidates > 0`. Safety cap 10M rounds. Default resume cursor `shield of the realm`.

2025-03-24 – Phase 3 backfill loop script:
- `frontend/run-phase3-backfill-loop.mjs` — removed verification-unchanged stop; resume cursor updated; 300-round run completed against local cron.

2025-03-24 – Phase 3C Bucket A SQL:
- `db/scryfall_cache_phase3c_bracketed_merge_review.sql` — full side-by-side dirty/canonical values; `bucket_a_count` = distinct dirty Bucket A rows.
- `db/scryfall_cache_phase3c_bracketed_merge_cleanup.sql` — merge includes images, rarity, set, collector via COALESCE; `SCRYFALL_CACHE_PHASE3C_BRACKETED_MERGE.md` run order.

2025-03-24 – Phase 3D Bucket B canonicalization SQL:
- `db/scryfall_cache_phase3d_bucket_b_canonicalization.sql` — NOT IN exclusion list for malformed proposed PKs (Section 0 + Section 1).

2025-03-24 – Phase 3D Bucket B canonicalization (SQL only):
- `db/scryfall_cache_phase3d_bucket_b_review.sql`, `db/scryfall_cache_phase3d_bucket_b_canonicalization.sql`, `db/SCRYFALL_CACHE_PHASE3D_BUCKET_B.md` — safe-candidate rules; INSERT canonical row (lower(trim inner)); no delete; ON CONFLICT DO NOTHING; Phase 3 resume note.

2025-03-24 – Phase 3C review SQL UX:
- `db/scryfall_cache_phase3c_bracketed_merge_review.sql` — note: run first counts `SELECT` alone (Supabase often returns only last statement); remove duplicate `bucket_b_count` query; add `total_bracketed_non_mdfc`.

2025-03-24 – Phase 3C two-bucket bracketed cleanup (SQL only):
- `db/scryfall_cache_phase3c_bracketed_merge_review.sql` — Bucket A (with canonical) vs Bucket B (no canonical) counts; A side-by-side; B review + triage columns; no deletes for B.
- `db/scryfall_cache_phase3c_bracketed_merge_cleanup.sql` — Bucket A merge (rules/identity fields only; not images/set/rarity); `pick` restricted to rows with canonical; delete after merge.
- `db/SCRYFALL_CACHE_PHASE3C_BRACKETED_MERGE.md` — two-bucket plan and Bucket B next steps.

2025-03-24 – Phase 3C bracketed merge-then-delete (SQL only):
- `db/scryfall_cache_phase3c_bracketed_merge_review.sql`, `db/scryfall_cache_phase3c_bracketed_merge_cleanup.sql`, `db/SCRYFALL_CACHE_PHASE3C_BRACKETED_MERGE.md` — merge `[[...]]` dupes into canonical then delete; COALESCE-only fills.

2025-03-24 – Phase 3B scryfall_cache duplicate review (SQL only):
- `db/scryfall_cache_phase3b_duplicate_cleanup_review.sql`, `db/scryfall_cache_phase3b_duplicate_cleanup.sql`, `db/SCRYFALL_CACHE_PHASE3B_DUPLICATES.md` — bracketed `[[...]]` and print-annotation dupes vs existing canonical PK; commented safe deletes.

2025-03-24 – Phase 3 scryfall_cache backfill (Phase 2A + legalities):
- mergeScryfallCacheRowFromApiCard + needsPhase3Backfill; cron POST /api/cron/scryfall-cache-phase3-backfill (Scryfall collection, resumable cursor).
- db/scryfall_cache_phase3_verification.sql, db/SCRYFALL_CACHE_PHASE3.md.
- **build:** bulk-scryfall — `ScryfallCard` passed to `buildScryfallCacheRowFromApiCard` via `as unknown as Record<string, unknown>` for strict tsc.
- Phase 3 backfill route: dedupe merged rows by PK before upsert (fixes Postgres “ON CONFLICT … second time” when Scryfall returns duplicate names in one batch); response includes `duplicatePkInBatch`.
- Phase 3 resume: `nextAfter` is last **examined** row in the page (not last row of the 200 window) so the next `name > after` scan does not skip rows when the batch fills early.

2025-03-24 – Phase 2B scryfall_cache image writes:
- Image-cache DB upserts use resolved canonical Scryfall oracle name (normalizeScryfallCacheName(card.name)), not the request key; partial rows unchanged.
- getImagesForNamesCached / prewarm / batch-images: sanitizeImageCacheInputName for obvious junk; getImagesForNames Map keys unchanged for callers.

2025-09-10  –  Fix collections +/- & delete:
- API now accepts id, delta from JSON body OR query string for PATCH/DELETE.
- Auto-delete row when qty goes to 0 or below.
2025-09-10 – Improve /api/collections/cards PATCH:\n- Accept id via id|cardId|card_id (body or query).\n- Accept delta via delta|d|qtyDelta (body or query).\n- Support ?op=inc|dec, ?inc=1, ?dec=1, and delta='+'/'-'.\n- Support absolute qty mode: { id, qty }.\n- Auto-delete on qty<=0.\n