# Keep lint focused on production website surfaces.
# Admin pages, tests, and tooling currently contain legacy patterns (any, require, raw fetch)
# that are out-of-scope for conversion UI work like the mobile sticky banner.

app/admin/**
tests/**
tools/**
types/**

