'use client';

import RecentPublicDecks from './RecentPublicDecks';

export default function LeftSidebar() {
  return (
    <div className="w-full flex flex-col gap-4">
      {/* Recent Decks card */}
      <section className="rounded-xl bg-[#0B1220] border border-gray-800">
        <header className="px-4 pt-3 pb-2 text-sm font-semibold text-gray-200">
          Recent Decks
        </header>

        <div className="px-2 pb-3">
          <RecentPublicDecks />
        </div>
      </section>

      {/* Ad slot (unchanged) */}
      <div className="rounded-xl border border-dashed border-gray-700 p-6 text-center text-xs text-gray-500">
        AD PLACEHOLDER
        <br />300 × 250
      </div>
    </div>
  );
}
